# ui-capture (generated)

This folder holds **local** Simulator capture runs from `scripts/capture_watch_ui_flow.sh`.

- **Tracked in git:** this `README.md` only.
- **Not tracked:** timestamped subfolders (`*/screenshots`, `*.mp4`, `*.xcresult`) — see root `.gitignore`.

Copy artifacts to the machine where the agent runs (e.g. scp from Mac to omarchy) if you want them in the workspace for review.

See **`docs/ui-capture.md`** for the full runbook.
